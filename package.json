{
  "name": "growi-plugin-visual-editor",
  "version": "0.0.1",
  "description": "Visual (WYSIWYG) editor plugin for GROWI - technical verification spike",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "prosemirror-markdown": "^1.13.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  },
  "growiPlugin": {
    "schemaVersion": "4",
    "types": [
      "script"
    ]
  }
}
